{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Array of Enums",
  "type": "object",
  "properties": {
    "statuses": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["NEW", "PROCESSING", "DONE"]
      }
    }
  },
  "required": ["statuses"]
}
